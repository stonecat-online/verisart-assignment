import styled from "styled-components"
import { device } from "globals/mediaQueries"

export const Wrapper = styled.div`
  /* // styling
  font-family: var(--averta-bold), sans-serif;
  color: var(--main-bg-color);
  @media ${device.laptop} {
    color: grey;
  } */
`
